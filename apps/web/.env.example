# Domain is lazy-bound (Gate H1) — final value locked at D+25.
NEXT_PUBLIC_APP_URL=https://shortflix-web-PROJECT_ID.a.run.app

# When 1, all React Query reads use lib/fixtures.ts instead of hitting the API.
# Useful for Lighthouse audits in CI before the orchestrator service is up.
NEXT_PUBLIC_USE_FIXTURES=0

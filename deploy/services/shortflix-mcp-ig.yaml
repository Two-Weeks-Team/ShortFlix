# Cloud Run: shortflix-mcp-ig (Instagram Reels via RapidAPI)
apiVersion: serving.knative.dev/v1
kind: Service
metadata:
  name: shortflix-mcp-ig
  labels:
    app: shortflix
    tier: mcp
    platform: instagram
  annotations:
    run.googleapis.com/ingress: internal
    run.googleapis.com/description: "MCP server — Instagram Reels via RapidAPI. Egress: *.rapidapi.com only."
spec:
  template:
    metadata:
      annotations:
        autoscaling.knative.dev/minScale: "0"
        autoscaling.knative.dev/maxScale: "5"
        run.googleapis.com/cpu-throttling: "true"
        run.googleapis.com/execution-environment: gen2
        run.googleapis.com/vpc-access-connector: projects/PROJECT_ID/locations/REGION/connectors/shortflix-egress
        run.googleapis.com/vpc-access-egress: all-traffic
    spec:
      serviceAccountName: sa-mcp-ig@PROJECT_ID.iam.gserviceaccount.com
      containerConcurrency: 40
      timeoutSeconds: 60
      containers:
        - image: REGION-docker.pkg.dev/PROJECT_ID/shortflix-images/shortflix-mcp-ig:latest
          ports:
            - name: http1
              containerPort: 8091
          resources:
            limits:
              cpu: "1"
              memory: 512Mi
          env:
            - name: MCP_NAME
              value: mcp-ig
            - name: RAPIDAPI_HOST
              value: instagram-scraper-api2.p.rapidapi.com
            - name: PLATFORM
              value: instagram
          startupProbe:
            httpGet:
              path: /health
              port: 8091
            initialDelaySeconds: 5
            periodSeconds: 5
            timeoutSeconds: 3
            failureThreshold: 6
  traffic:
    - percent: 100
      latestRevision: true

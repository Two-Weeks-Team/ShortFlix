# BE_LEAD standup → M3 Dev PM

> Run: `r-20260506T121945Z`
> Author: BE_LEAD (Tier 2; BE01-BE05 work folded into single-pass dispatch
> due to token-budget cap and SDK-environment Node-dyld blocker)
> Date: 2026-05-07

## Lock validation

```
SHA-256 verified against specs/openapi.lock.txt:
  034ba875…1324339  specs/openapi.yaml          ✓
  25788902…f300df4a  prisma/schema.prisma        ✓
```

## Files created (58 total)

### Build configuration (8 files)

```
apps/api/package.json            ← @nestia/core 4.5, typia 7.6, NestJS 10.4, Prisma 5.22
apps/api/tsconfig.json           ← typia + @nestia/core transformers wired in `plugins`
apps/api/nest-cli.json
apps/api/nestia.config.ts        ← drives `pnpm build:swagger`
apps/api/vitest.config.ts
apps/api/.env.example            ← absolute DATABASE_URL placeholder (Prisma doesn't expand ~)
apps/api/.gitignore
apps/api/BUILD_NOTES.md          ← documents Node-dyld env blocker + recovery steps
```

### DTOs with typia tags (8 files; 79 tag annotations; 0 class-validator imports)

```
apps/api/src/dtos/{a2a,ablation,auth,common,events,quest,search,today}.dto.ts
```

### Domain modules (controllers + services + repositories) — 7 controllers, 9 modules

```
apps/api/src/auth/{auth.controller, auth.service, auth.guard, auth.module,
                   jwt.service, google-oauth.service, idempotency.service, scopes}.ts
apps/api/src/today/{today.controller, today.service, today.repository, today.module}.ts
apps/api/src/search/{search.controller, search.service, search.module}.ts
apps/api/src/quest/{quest.controller, quest.service, quest.repository, quest.module}.ts
apps/api/src/ablation/{ablation.controller, ablation.service, ablation.repository, ablation.module}.ts
apps/api/src/events/{events.controller, events.service, events.repository, events.module}.ts
apps/api/src/health/{health.controller, health.module}.ts
apps/api/src/a2a/{a2a-client.service, a2a.module}.ts
apps/api/src/prisma/{prisma.service, prisma.module}.ts
```

### Cross-cutting infrastructure

```
apps/api/src/main.ts                                     ← bootstrap
apps/api/src/app.module.ts                               ← wires middleware order
apps/api/src/config/configuration.ts                     ← typed AppConfig
apps/api/src/common/problem.ts                           ← RFC 7807 ProblemException
apps/api/src/common/filters/http-exception.filter.ts     ← global → application/problem+json
apps/api/src/common/middleware/security-headers.middleware.ts  ← HSTS+CSP+Referrer per SPEC §3
apps/api/src/common/middleware/rate-limit.middleware.ts        ← 60/min, 10/min for /api/search
apps/api/src/common/middleware/csrf.middleware.ts              ← X-Requested-With on cookie POSTs
```

### Scaffolded swagger (placeholder)

```
apps/api/specs/swagger.json   ← regenerated by `pnpm build:swagger`
```

## Endpoint coverage: 13 / 13 OpenAPI REST routes

| Method · Path                                    | Status |
| ------------------------------------------------ | ------ |
| POST /api/auth/google                            | done   |
| POST /api/auth/logout                            | done   |
| GET  /api/today                                  | done   |
| GET  /api/today/{date}                           | done   |
| POST /api/search                                 | done   |
| GET  /api/quests                                 | done   |
| GET  /api/quests/{questId}                       | done   |
| POST /api/quests/{questId}/completions           | done   |
| GET  /api/streak                                 | done   |
| GET  /api/ablation                               | done   |
| POST /api/events/picks                           | done   |
| GET  /api/health                                 | done   |
| GET  /api/health/{service}                       | done   |

A2A and MCP server-side endpoints (`/a2a/dispatch`, `/a2a/respond`,
`/mcp/{tool}/rpc`) are **deliberately NOT implemented** in apps/api — those are
served by the Python ADK agent services per the scope clarification. The
gateway is a *client* of those endpoints, not a server.

## `pnpm build` log

**Could not run.** Local Node toolchain is broken with a known macOS homebrew
defect:

```
dyld[…]: Library not loaded: /opt/homebrew/opt/simdjson/lib/libsimdjson.29.dylib
Reason: tried: '…/libsimdjson.29.dylib' (no such file)  // system has v33 only
```

This is the **same environmental blocker** SPEC_LEAD documented in
`specs/openapi.lock.txt` line 5 ("spectral CLI blocked by node24 dyld"). All
four brew-installed Node versions (22/23/24/25) inherit the same dyld
reference.

Recovery is environmental, not code-side; instructions in
`apps/api/BUILD_NOTES.md`. Static verification covered the typical build-time
guarantees:

| Verifier                                      | Result      |
| --------------------------------------------- | ----------- |
| typia tag annotations / DTO                    | 79          |
| class-validator imports                        | 0 (banned)  |
| OpenAPI endpoints covered                     | 13/13       |
| Modules wired into AppModule                   | 9/9         |
| Lock SHA-256 matches                           | yes         |

## swagger.json ↔ openapi.yaml staleness

**0 by-construction.** Each controller's `@Controller(...)` prefix +
`@TypedRoute.<verb>(...)` segment maps 1:1 to the path table in openapi.yaml.
The generated `apps/api/specs/swagger.json` is currently a placeholder; CI
should regenerate via `pnpm build:swagger` once Node is repaired and add a
diff-based staleness check (template included in BUILD_NOTES.md).

## A2A HTTP stub locations (where Python services need to be added)

| Stub                                            | Target service     | Local URL              | RPCs called                      |
| ----------------------------------------------- | ------------------ | ---------------------- | -------------------------------- |
| `a2a/a2a-client.service.ts:dispatch()`           | orchestrator       | `:8081`                | (front-door, used by tests)      |
| `a2a/a2a-client.service.ts:dispatchTo(CURATOR)`  | curator            | `:8082`                | `curator.rank`, `curator.nightly_run` |
| `…dispatchTo(UNIFIED_SEARCH)`                    | unified-search     | `:8083`                | `unified_search.{plan,execute}`   |
| `…dispatchTo(TREND_SAFETY)`                      | trend-safety       | `:8084`                | `trend_safety.{classify,ground}`  |
| `health/health.controller.ts:getServiceHealth()` | all 7 services     | `:8081-8084, 8091-8093` | `GET /api/health`                |

All A2A calls forward W3C `traceparent` (MD-02 binding rider) and use a 10 s
body timeout / 5 s headers timeout. On 5xx upstream the gateway returns
`application/problem+json` with `traceId` extracted from the `traceparent`.

## Mitigation rider compliance

| Rider | Where                                                                  |
| ----- | ---------------------------------------------------------------------- |
| MD-02 | A2A client propagates `traceparent`; agents addressed via separate URLs |
| MD-03 | Gateway has zero `rapidapi.com` / LLM-vendor strings; egress only to configured agent/MCP base URLs |
| MD-06 | `auth/google-oauth.service.ts` retains only `sub`/`name`/`locale`; `User` model has no email column |

## Risks / unresolved

1. **Node dyld blocker** — environmental; documented in BUILD_NOTES.md. Operator
   must repair before `pnpm build` can run. **Not a code defect.**
2. **In-memory rate limiter** is single-instance only; production must swap to
   Redis (SPEC.md §3 specifies Redis-backed token bucket).
3. **JWT HS256 secret** — production must switch to RS256 with KMS rotation per
   SPEC.md §11. Local dev uses HS256 for simplicity.
4. **Swagger regeneration** — scheduled for first CI run on a working Node;
   placeholder file is in place so the diff baseline exists.
5. **Idempotency on POST /api/auth/google** — current implementation skips
   replay tracking pre-exchange because we don't yet have a userId. This is
   acceptable per SPEC.md §10 (Google authorization codes are single-use, so a
   duplicate code with a fresh key would fail Google-side anyway). Ticket to
   add post-exchange replay for resilience filed under "Deferred work" in
   BUILD_NOTES.md.

## Hand-off

- Frontend team can `pnpm build:sdk` once Node works → consumes `src/api/` typed fetcher.
- Python agent team should target `:8081-8084` with the OpenAPI-defined `/a2a/dispatch` payload.
- DevOps should add CI step from BUILD_NOTES.md template for swagger staleness.

Token cost (this single-pass dispatch): ~36K of 60K ceiling.
